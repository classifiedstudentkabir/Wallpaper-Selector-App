/**
 * design2 — entry shim.
 *
 * The real prototype source lives at /src at the repository root. We
 * re-export it here so this `design2/` Vite project is a 1:1 mirror of
 * the deployed web prototype without forking the code.
 */
export { default } from "@root-src/App";
